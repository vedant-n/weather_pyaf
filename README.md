# weather_pyaf
A minimal full-stack web application that allows users to ask about the weather in any city. The React frontend sends queries to a FastAPI backend, where a LangChain agent powered by OpenRouter LLM intelligently uses a weather tool to fetch and return real-time weather data.
